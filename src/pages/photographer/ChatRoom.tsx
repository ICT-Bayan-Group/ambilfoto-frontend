import ChatRoom from "@/pages/user/ChatRoom";

// Photographer chat room reuses the same component — 
// it auto-detects the user role and adjusts back navigation
const PhotographerChatRoom = ChatRoom;

export default PhotographerChatRoom;
